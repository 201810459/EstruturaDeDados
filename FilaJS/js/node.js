//Criada a classe Node(nó), a qual o construtor recebe um elemento e cria um content que recebe o elemento
//e um next que recebe null
class Node{
    constructor(element){
        this.content = element;
        this.next = null;
    }
}